<div class="video-card">
  <%= link_to video_path(video) do %>
  <img src="https://img.youtube.com/vi/<%= video.url %>/hqdefault.jpg" alt="Card image cap">
  <div class="video-card-body">
    <h5><%= video.title %></h5>
    <p><%= video.category %> </p>
    <p> <%= video.created_at.strftime("%d de %B de %Y") %></p>
  </div>
  <%end %>
</div>

