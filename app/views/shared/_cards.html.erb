<div class="article-cards">
  <%= link_to article_path(article) do %>
  <img src="<%= article.photo %>" alt="">
  <div class="article-content">
    <div>
      <h2><%= article.title %></h2>
      <p><%= article.subtitle.truncate(80) %></p>
    </div>
  </div>
  <% end %>
</div>
